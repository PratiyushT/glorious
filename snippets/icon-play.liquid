{%- assign icon_width = width | default: 14 -%}
{%- assign icon_height = height | default: 14 -%}
<svg class="icon-svg{% if class != blank %} {{ class }}{% endif %}" width="{{ icon_width }}" height="{{ icon_height }}" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true" focusable="false"><path d="M6 3.8 16 10 6 16.2z" /></svg>
