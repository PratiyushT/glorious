{%- assign icon_width = width | default: 14 -%}
{%- assign icon_height = height | default: 14 -%}
<svg class="icon-svg{% if class != blank %} {{ class }}{% endif %}" width="{{ icon_width }}" height="{{ icon_height }}" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" aria-hidden="true" focusable="false"><path d="M10 3v14M3 10h14" /></svg>
