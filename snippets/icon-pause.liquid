{%- assign icon_width = width | default: 14 -%}
{%- assign icon_height = height | default: 14 -%}
<svg class="icon-svg{% if class != blank %} {{ class }}{% endif %}" width="{{ icon_width }}" height="{{ icon_height }}" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true" focusable="false"><path d="M5 4h3.4v12H5zM11.6 4H15v12h-3.4z" /></svg>
