{%- assign icon_width = width | default: 30 -%}{%- assign icon_height = height | default: 30 -%}
<svg class="icon-svg{% if class != blank %} {{ class }}{% endif %}" width="{{ icon_width }}" height="{{ icon_height }}" viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M5 8h30L24 21v10l-8 4V21z" /></svg>
