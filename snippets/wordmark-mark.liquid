{%- comment -%}
  Compatibility wrapper for the centralized SVG library.

  Existing callers keep the semantic wordmark-mark name; all SVG markup now
  lives with the icon-*.liquid family and is reached through icon.liquid.
{%- endcomment -%}
{% render 'icon', icon: 'wordmark-mark', class: class %}
