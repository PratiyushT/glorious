<div class="page-width simple-template">
  <div class="stack stack--center measure measure--center">
    <h1 class="h2">{{ '404.title' | t }}</h1>
    <p class="text-muted">{{ '404.subtext' | t }}</p>
    <a class="btn btn--primary" href="{{ routes.root_url }}">{{ 'general.continue_shopping' | t }}</a>
  </div>
</div>
